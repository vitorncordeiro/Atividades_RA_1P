#Faça uma função que informe a quantidade de dígitos de um determinado número inteiro informado.
def contarDigitos(num:str):
    leng = len(num)
    return leng
num1 = input('Digite o número: ')
print(contarDigitos(num1))
